# 引数priceに引数taxを加算し、その値を出力する関数を作成する
def purchased_price(price: int, tax: int):
    purchased_price = price + tax

    print(f"{purchased_price}円")

# 関数を呼び出し、引数として、商品の金額と消費税額を渡す
purchased_price(100, 10 )